import 'package:flutter/material.dart';

class AppBlockRow extends StatelessWidget {
  const AppBlockRow({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
